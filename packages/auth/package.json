{
	"name": "@politigraph/auth",
	"type": "module",
	"private": true,
	"scripts": {
		"generate": "bun run --bun better-auth generate -y && bun run --bun drizzle-kit generate",
		"migrate": "bun migrate.ts",
		"lint": "eslint . --fix",
		"check": "tsc --noEmit",
		"seed": "bun run seed.ts"
	},
	"dependencies": {
		"@politigraph/config": "workspace:*",
		"better-auth": "~1.4.22",
		"drizzle-kit": "^0.31.11",
		"drizzle-orm": "^0.45.3",
		"postgres": "^3.4.9"
	},
	"devDependencies": {
		"@better-auth/cli": "^1.4.21"
	},
	"peerDependencies": {
		"typescript": "^5.9.3"
	}
}
