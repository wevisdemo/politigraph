{
	"name": "@politigraph/auth",
	"type": "module",
	"private": true,
	"scripts": {
		"generate": "bun run --bun better-auth generate -y && bun run --bun drizzle-kit generate",
		"migrate": "bun migrate.ts"
	},
	"dependencies": {
		"@better-auth/cli": "^1.3.34",
		"better-auth": "^1.3.34",
		"drizzle-kit": "^0.31.7",
		"drizzle-orm": "^0.44.7",
		"postgres": "^3.4.7"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}
