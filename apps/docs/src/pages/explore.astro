---
import StarlightPage from '@astrojs/starlight/components/StarlightPage.astro';
import ExploreGraph from '../components/explore/explore-graph.vue';
---

<StarlightPage
	frontmatter={{
		title: 'สำรวจข้อมูล',
		template: 'splash',
		tableOfContents: false,
		pagefind: false,
		prev: false,
		next: false,
	}}
>
	<ExploreGraph client:load />
</StarlightPage>

<style is:global>
	/* Immersive explore: only the Starlight navbar, search bar, and graph */
	:root:not([data-has-sidebar]) {
		--sl-content-width: 100%;
	}

	.main-frame {
		padding-top: var(--sl-nav-height);
		padding-inline-start: 0;
	}

	main {
		padding: 0;
	}

	.content-panel {
		padding: 0;
		border: none;
	}

	/* Hide the page title panel and Starlight's footer meta */
	.content-panel:first-of-type,
	main footer {
		display: none;
	}

	.content-panel .sl-container {
		max-width: none;
		margin: 0;
	}

	/* Breathing room around the WeVis footer, only on this page */
	.content-panel:last-of-type .sl-container > div:last-child {
		margin: 1rem var(--sl-content-pad-x);
	}
</style>
